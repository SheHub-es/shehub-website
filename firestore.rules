rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /applicants_v2/{docId} {
      allow create, update: if true;
      allow read, delete: if false;
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
